# CODEOWNERS — 경로별 소유자·필수 리뷰어 자동 배정
#
# 동작: PR이 아래 경로를 건드리면 해당 소유자가 필수 리뷰어로 자동 배정된다.
#       (main 브랜치 보호 규칙에서 "Require review from Code Owners"를 켜야 강제된다)
#
# 자기 영역 PR은 상대방이 리뷰하고, 공유 영역은 둘 다 배정돼 상대방 승인으로 머지한다.

# ─── 협업 모드 ────────────────────────────────────────────
frontend/    @leemonta9482
backend/     @ssenu
deploy/      @ssenu @leemonta9482
finetune/    @ssenu @leemonta9482
docs/        @ssenu @leemonta9482
.github/     @ssenu @leemonta9482
/CLAUDE.md   @ssenu @leemonta9482
/.pre-commit-config.yaml  @ssenu @leemonta9482

# 계약 파일은 백엔드가 소유하되 프론트도 알아야 하므로 둘 다 배정
backend/data/manifest.json  @ssenu @leemonta9482
docs/API.md                 @ssenu @leemonta9482

# ─── 개인 모드 (혼자가 될 경우) ──────────────────────────────
# 위 경로별 규칙을 지우고 아래 한 줄로 축약한다.
# 소유자가 한 명이면 경로별 배정이 무동작이므로 단순화.
#
# * @ssenu
#
# 계층 규율(고비용 계층 보호)은 승인 게이트가 아니라 자기 규율로 남는다 —
# 근거·절차는 루트 CLAUDE.md "계층 규율" 참조. 여기서 강제하지 않는다.
